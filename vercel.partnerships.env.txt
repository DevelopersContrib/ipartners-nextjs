# Add these in Vercel → Project → Settings → Environment Variables
# Use the same VNOC_INGEST_KEY value as manage-app.

VNOC_INGEST_KEY=
# Optional override (defaults to https://api.vnoc.com/api/v1/partnerships)
# VNOC_PARTNERSHIP_INGEST_URL=

# analytics.vnoc.com — PartnerScore traffic (UV/PV 7d + 30d). Same key as manage-app.
VNOC_ANALYTICS_KEY=
# VNOC_ANALYTICS_URL=https://analytics.vnoc.com
# Public tracker (all pages via layout) — no key required:
# NEXT_PUBLIC_VNOC_ANALYTICS_URL=https://analytics.vnoc.com
# NEXT_PUBLIC_VNOC_ANALYTICS_ENABLED=true


NEXT_PUBLIC_DOMAIN=ipartner.com
NEXT_PUBLIC_BASE_URL=https://ipartner.com

# Comma-separated emails allowed into /admin (email-code login, same as partners).
# ADMIN_EMAILS=you@example.com,ops@example.com

# SES campaign emails (lifecycle: applied / approved / declined / active / lapsed).
# Uses existing AWS_* + SES_FROM_EMAIL. Only set CAMPAIGN_FROM_EMAIL after that
# identity is verified in SES (no-reply@ipartner.com is not verified yet).
#
# DNS (~10 min): SES → verify domain ipartner.com → publish DKIM CNAMEs → Verified.
# Also clear SES sandbox/probation in the console before volume sends.
# CAMPAIGN_FROM_EMAIL=no-reply@ipartner.com
# CAMPAIGN_EMAILS_ENABLED=true
# Apply send log: npm run db:apply prisma/migrations/0004_ipp_campaign_send.sql
#
# Nightly reconcile: approved → active when MarketPartnership is published (RO).
# vercel.json cron → /api/cron/reconcile-active (Bearer CRON_SECRET)
# CRON_SECRET=
# Local dry-run: pnpm run reconcile:active -- --dry-run
#
# Auto-messaging nudges (pending 3d+ / approved 7d+) — same CRON_SECRET
# GET /api/cron/auto-messages @ 14:30 UTC
# AUTO_MESSAGES_ENABLED=true
# Local: pnpm run auto:messages -- --dry-run

# Help center Ask AI (/portal/help) — optional. Without it, answers use ranked guides.
# OPENAI_API_KEY=
# OPENAI_HELP_MODEL=gpt-4o-mini
# Admin fraud auto-decline (/admin) uses the same key; optional model override:
# OPENAI_FRAUD_MODEL=gpt-4o-mini

# Admin triage enrichment + manage-app publish handoff (read-only MarketPartnership).
# FULLCONTACT_API_KEY=
# MANAGE_APP_BASE_URL=https://manage.vnoc.com

# Partner agent (Deals chat) + Growagent push on approve
# AGENT_AUTO_APPROVE=false
# GROWAGENT_BASE_URL=https://vnoc.growagent.com
# GROWAGENT_CAMPAIGN_ID=
# GROWAGENT_INGEST_SECRET=
# GROWAGENT_PUSH_ENABLED=true

# Support tickets / @contrib/mail — SES only (no Resend package)
EMAIL_PROVIDER=ses

# PayDirect sponsor checkout
# NEXT_PUBLIC_PAYDIRECT_API_KEY=pd_live_...
# PAYDIRECT_WEBHOOK_SECRET=whsec_...
# Webhook: https://ipartner.com/api/webhooks/paydirect
