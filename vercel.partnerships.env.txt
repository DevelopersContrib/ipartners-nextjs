# Add these in Vercel → Project → Settings → Environment Variables
# Use the same VNOC_INGEST_KEY value as manage-app.

VNOC_INGEST_KEY=
# Optional override (defaults to https://api.vnoc.com/api/v1/partnerships)
# VNOC_PARTNERSHIP_INGEST_URL=

# analytics.vnoc.com — PartnerScore traffic (UV/PV 7d + 30d). Same key as manage-app.
VNOC_ANALYTICS_KEY=
# VNOC_ANALYTICS_URL=https://analytics.vnoc.com
# Public tracker (all pages via layout) — no key required:
# NEXT_PUBLIC_VNOC_ANALYTICS_URL=https://analytics.vnoc.com
# NEXT_PUBLIC_VNOC_ANALYTICS_ENABLED=true


NEXT_PUBLIC_DOMAIN=ipartner.com
NEXT_PUBLIC_BASE_URL=https://ipartner.com

# Comma-separated emails allowed into /admin (email-code login, same as partners).
# ADMIN_EMAILS=you@example.com,ops@example.com

# SES campaign emails (lifecycle: applied / approved / declined / active / lapsed).
# Uses existing AWS_* + SES_FROM_EMAIL. Only set CAMPAIGN_FROM_EMAIL after that
# identity is verified in SES (no-reply@ipartner.com is not verified yet).
# CAMPAIGN_FROM_EMAIL=
# CAMPAIGN_EMAILS_ENABLED=true
# Apply send log: npm run db:apply prisma/migrations/0004_ipp_campaign_send.sql

# Help center Ask AI (/portal/help) — optional. Without it, answers use ranked guides.
# OPENAI_API_KEY=
# OPENAI_HELP_MODEL=gpt-4o-mini
